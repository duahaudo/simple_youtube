import { color_icon_menu_active, color_icon_menu_default, color_grey1, color_primary, mVal, padVal } from './../../style';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonWrapper: {
    backgroundColor: color_grey1,
    margin: mVal,
    padding: padVal,
    borderRadius: padVal
  },
  button: {
    justifyContent: "center",
    alignItems: "center"
  },
  buttonText: {

  }
});
