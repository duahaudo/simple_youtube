import {StyleSheet, Platform} from 'react-native';
import {color_primary, mVal} from '../../style';

export default StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 10,
  },
  spiner: {
    paddingVertical: mVal,
  },
});
